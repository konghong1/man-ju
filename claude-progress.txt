2026-04-25
- Initialized project plan from Anthropic harness principles.
- Added design document and feature checklist skeleton.
- Implemented Spring Boot backend (generation service, REST API, file repository).
- Implemented web UI at `/` for prompt input and structured result rendering.
- Added `init.sh` and `init.ps1` for harness-style bootstrap/health checks.
- Added integration tests for create/fetch flow, health endpoint, and welcome-page routing.
- Verification:
  - `mvn test` passed.
  - `powershell -ExecutionPolicy Bypass -File .\init.ps1` passed.
- Re-planned architecture with detailed module-level design and task decomposition:
  - Added `docs/design-detailed.md`.
  - Added `docs/task-breakdown-v2.md`.
  - Rebuilt `feature_list.json` into granular multi-module backlog with status fields.
- Updated planning per review feedback:
  - Frontend stack switched to Vue3 + TypeScript + Vite + Pinia.
  - Added task flowcharts in `docs/task-flowchart-v2.md`.
- Upgraded to V3 multi-provider design:
  - Added LLM Gateway architecture (routing/fallback/circuit-breaker/cost/metrics).
  - Expanded task breakdown with dedicated LLM module tasks.
  - Updated feature backlog to include provider-level execution items.
- Task FE-001 completed:
  - Scaffolded `frontend/` with Vue3 + TypeScript + Vite.
  - Added route skeleton for project pages and model settings.
  - Verified with `npm run build` in frontend.
- Task FE-002 completed:
  - Added Pinia and persisted stores: `project/job/llm/ui`.
  - Added route-aware UI state sync and persisted panel toggles.
  - Verified with `npm run build` in frontend.
- Task FE-003 completed:
  - Implemented workspace draft store with Scene/Panel editable data.
  - Added Scene Editor + Panel Editor UI in workspace route.
  - Edits are persisted via Pinia local-storage plugin and visible immediately.
  - Verified with `npm run build` in frontend.
- Task FE-004 completed:
  - Implemented model settings UI for provider enable/disable and model selection.
  - Added step-level route mapping editor in `/settings/models`.
  - Added runtime metrics table and refresh interaction.
  - Verified with `npm run build` in frontend.
- Task BE-001 completed:
  - Added S1~S7 harness orchestrator with per-step trace capture.
  - Added step records with provider/model/tokens/cost/latency/error fields.
  - Persisted latest job ID and generation trace in `ComicProject`.
  - Verified with `mvn test`.
- Task LLM-001 completed:
  - Added unified `LlmClient` contract with `chat/structured/stream`.
  - Implemented simulated provider clients (`openai/anthropic/gemini/internal`).
  - Added `LlmClientRegistry` and wired orchestrator to provider contract.
  - Verified with `mvn test`.
- Task LLM-002 completed:
  - Added model catalog service and step-level routing policy service.
  - Added LLM config APIs for providers/models and project route updates.
  - Wired orchestrator provider selection to configurable routing policy.
  - Verified with `mvn test`.
- Task LLM-003 and LLM-004 completed:
  - OpenAI and Anthropic adapters are executable via unified `LlmClient`.
  - Harness default routes execute S2 with OpenAI and S3 with Anthropic.
  - Integration tests now assert provider trace for both steps.
  - Verified with `mvn test`.
- Task LLM-005 completed:
  - Added project rerun endpoint `POST /api/projects/{id}/jobs` to re-execute harness on existing input.
  - Added route configuration flow + test proving `S2_STORY_PLAN -> gemini` takes effect.
  - Verified with `mvn test`.
- Task LLM-014 completed:
  - Added provider state management APIs: list and patch enable/disable.
  - Added provider-state-aware routing fallback (disabled provider falls back to internal).
  - Extended integration tests for provider disable fallback behavior.
  - Verified with `mvn test`.
- Task LLM-006/007/008 completed:
  - Added provider adapters for `azure-openai`, `openai-compatible`, and `ollama`.
  - Extended model catalog entries for the new providers.
  - Added integration test matrix to verify route -> provider execution for all three.
  - Verified with `mvn test`.
- Task LLM-009 and LLM-012 completed:
  - Added provider fallback execution service with per-provider fallback chains.
  - Routed harness LLM invocation through fallback service.
  - Added tests for primary-disabled fallback and full external-disabled fallback.
  - Step trace now consistently records token/cost metrics from provider calls.
  - Verified with `mvn test`.
